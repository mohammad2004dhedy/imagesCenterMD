import HistoryClient from "@/components/history/HistoryClient";

export const metadata = {
  title: "Search History | MDImages"
};

export default function HistoryPage() {
  return <HistoryClient />;
}
